

@typeparam TKey
@inherits AknTreeListViewBase<TKey>

@using MudBlazor
@namespace NxRebuild.Client.Pages.NxPrograms.MDI.Tree_List_View

@* 抽象フォームの画面レイアウトを定義するコンポーネントです。 
　　これをテンプレートとして具象フォームを作成することができます。*@

<style>
    /* 画面全体のレイアウト（左右並び・隙間・高さ） */
    .app-container {
        display: flex;
        flex-direction: row;
        gap: 10px;
        flex: 1;
        min-height: 0;
        min-width: 0;
        width: 100%;
        overflow: hidden;
        color: black;
        padding: 5px;
        box-sizing: border-box;
    }

    @@media (max-width: 700px) {
        .app-container {
            flex-direction: column;
        }

        .tree-pane {
            flex: 0 0 25% !important;
            width: 100% !important;
            max-height: 25% !important;
        }

        .grid-pane {
            flex: 1 !important;
            width: 100% !important;
        }
    }

    .scroll-pane {
        background-color: white;
        border: 1px solid #ccc;
        padding: 10px;
        display: flex;
        flex-direction: column;
        min-height: 0;
        overflow-y: auto;
        overflow-x: auto;
    }

    .tree-pane {
        flex: 0 0 300px;
        min-width: 250px;
        min-height: 0;
    }

    .grid-pane {
        flex: 2;
        min-width: 0;
        min-height: 0;
    }
</style>

<div class="app-container">
    @* A. ツリーコンポーネント *@
    <AknTreeView TKey="int"
                 TreeData="TreeData"
                 OnNodeSelected="HandleNodeSelection"
                 OnNodeDropped="HandleNodeDrop"
                 @ref="_treeview" />

    @* B. グリッドコンポーネント *@
    <AknListView TKey="int"
                 ListDataItems="GridDataItems"
                 Columns="Columns"
                 AllowSorting="true"
                 OnRowClicked="HandleGridClick"
                 OnRowDoubleClicked="HandleGridDoubleClick"
                 OnRowDropped="HandleGridReorder"
                 OnSortRequested="HandleSortRequest"
                 @ref="_listview" />
</div>

@code {

}
